#include "BST.hpp"

int main()
{
    BST first;
    newNode(x);
    begin->setData(5);
    //std::cout << begin->getData() << "\n";
    first.setRoot(begin);
    std::cout << first.getRoot()->getData() << "\n";
    delete begin;
    begin = nullptr;
}
